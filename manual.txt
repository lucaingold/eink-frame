HTML-Server 80

sudo nohup python3 runWebserverImages.py > /dev/null 2>&1 &


Frame
git clone https://github.com/waveshare/IT8951-ePaper.git
cd IT8951-ePaper/Raspberry
sudo make clean
sudo make -j4

Add the VCOM value as a parameter and test the e-Paper, and this model is mode 0. 

sudo ./epd -2.27 0


sudo nohup python3 /home/luca/code/eink-frame/webserver.py > /dev/null 
sudo /home/luca/code/eink-frame/epd -2.27 0 &

1.
 sudo nano /etc/systemd/system/inkframe_startup.service
chmod +x /home/luca/code/eink-frame/startup.sh

 2.
[Unit]
Description=Ink Frame Startup
After=network.target

[Service]
Type=forking
ExecStart=/home/luca/code/eink-frame/startup.sh

[Install]
WantedBy=default.target

3.
sudo systemctl daemon-reload
sudo systemctl enable inkframe_startup.service
sudo systemctl start inkframe_startup.service




sudo apt-get install busybox
pip3 install flask-cors
pip3 install flask